<h1 class="mt-4">Articles Listing Page</h1>

<div class="flickr_pagination">
    <%= will_paginate @articles, :container => false %>
</div>
<%= render 'article' %>
<%= button_to "Create new Article",new_article_path, method: :get %>

<div class="flickr_pagination mb-4">
    <%= will_paginate @articles, :container => false %>
</div>
