<h1>Edit Article</h1>

<%# If there were any errors with posting the article, display
to the end user in an unordered list %>
<% if @article.errors.any? %>
<h2>The following errors prevented the article from being saved</h2>
<ul>
  <% @article.errors.full_messages.each do |msg| %>
  <li><%= msg %></li>
  <% end %>
  </ul>
<% end %>

<%# Change form scope to show current information to be edited %>
<%= form_with(model:@article, local: true) do |f| %>
    <p>
    <%= f.label :title %> <br>
    <%= f.text_field :title %>
    </p>

    <p>
    <%= f.label :description %> <br>
    <%= f.text_area :description %>
    </p>
    <p>
    <%= f.submit %>
    </p>
<% end %>
<%= link_to "Cancel edit and return to Articles listing", articles_path %>